/// ---------------------------------------------------------------------------
/// 模式：前 K 个元素（Top 'K' Elements）
/// ---------------------------------------------------------------------------
///
/// ## 核心原理
/// 使用大小为 K 的堆来高效找出数组中前 K 大/小的元素。
/// - 找前 K 大 → 大小为 K 的最小堆（堆顶是第 K 大）。
/// - 找前 K 小 → 大小为 K 的最大堆（堆顶是第 K 小）。
/// 优势：O(n log K) vs 全排序的 O(n log n)，当 K << n 时优势明显。
///
/// ## 适用场景
/// - 寻找第 K 大/小的元素。
/// - 前 K 个出现频率最高的元素。
/// - 从多个有序数组中找前 K 小的和。
/// - 数据流中实时维护 Top K。
///
/// ## 时间复杂度
/// - O(n log K)，每个元素最多一次堆操作。
///
/// ## 空间复杂度
/// - O(K)，堆中只存 K 个元素。

// ============================================================================
// LeetCode #215: Kth Largest Element in an Array（Medium）
// ============================================================================
// 题目描述：
//   给定整数数组 nums 和整数 k，返回数组中第 k 大的元素。
//   注意是排序后的第 k 大，不是第 k 个不同元素。
//
// 解题思路（大小为 k 的最小堆）：
//   遍历数组，维护一个大小为 k 的最小堆：
//   - 堆大小 < k → 直接入堆
//   - 堆大小 == k，且当前元素 > 堆顶 → 弹出堆顶，当前元素入堆
//   遍历完后，堆顶就是第 k 大的元素（堆中最小的那个，但比它小的 n-k 个都没进堆）
//
// 实现步骤：
//   1. 创建最小堆（List<int>），需要实现 add 和 removeTop 操作
//   2. 遍历 nums：
//      a. 如果 heap.length < k → add(num)
//      b. 否则如果 num > heap[0] → removeTop()，add(num)
//   3. 返回 heap[0]

int findKthLargest(List<int> nums, int k) {
  // TODO: 实现最小堆的 add 操作（上浮）
  // TODO: 实现最小堆的 removeTop 操作（下沉）
  // TODO: 遍历 nums，维护大小为 k 的最小堆
  // TODO: 返回 heap[0]
  throw UnimplementedError(); // 请替换为你的实现
}

// ============================================================================
// LeetCode #347: Top K Frequent Elements（Medium）
// ============================================================================
// 题目描述：
//   给定非空整数数组 nums，返回出现频率前 k 高的元素。
//
// 解题思路（HashMap 统计频率 + 最小堆）：
//   1. 统计每个元素的出现频率
//   2. 用大小为 k 的最小堆维护频率前 k 高的元素
//   3. 堆中按频率比较，频率低的在堆顶
//
// 实现步骤：
//   1. 遍历 nums，用 Map 统计频率
//   2. 遍历频率表，维护大小为 k 的最小堆（按频率比较）
//   3. 收集堆中所有元素的值，返回结果列表

List<int> topKFrequent(List<int> nums, int k) {
  // TODO: 统计频率 → Map<element, frequency>
  // TODO: 最小堆维护前 K 高频率（按频率比较）
  // TODO: 收集堆中元素返回
  throw UnimplementedError(); // 请替换为你的实现
}

// ============================================================================
// LeetCode #373: Find K Pairs with Smallest Sums（Medium）
// ============================================================================
// 题目描述：
//   给定两个升序数组 nums1 和 nums2，以及整数 k。返回和最小的 k 个数对 (u, v)，
//   其中 u 来自 nums1，v 来自 nums2。
//
// 解题思路（多路归并 + 最小堆）：
//   不用生成所有 m×n 个对，而是用堆做多路归并：
//   - 初始：每个 nums1[i] 与 nums2[0] 配对入堆（最多 k 个）
//   - 每次弹出堆顶（和最小的对）加入结果
//   - 然后对于弹出的对 (i, j)，把 (i, j+1) 入堆（同一路的下一对）
//   - 重复直到收集 k 个对或堆空
//
// 实现步骤：
//   1. 创建最小堆，元素含 (sum, i1, i2)
//   2. 初始化：对每个 nums1[i]（i < min(k, m)），加入 (nums1[i] + nums2[0], i, 0)
//   3. 循环 k 次（或堆空）：
//      a. 弹出堆顶，将 (nums1[i1], nums2[i2]) 加入结果
//      b. 如果 i2 + 1 < n → 加入 (nums1[i1] + nums2[i2+1], i1, i2+1) 到堆
//   4. 返回结果

List<List<int>> kSmallestPairs(List<int> nums1, List<int> nums2, int k) {
  // TODO: 实现最小堆的 add 和 removeTop
  // TODO: 初始化堆：nums1[i] + nums2[0] 配对入堆
  // TODO: 循环 k 次，弹出堆顶并加入新候补
  // TODO: 返回结果列表
  throw UnimplementedError(); // 请替换为你的实现
}
